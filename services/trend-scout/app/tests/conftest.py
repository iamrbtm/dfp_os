"""Pytest config for services/trend-scout tests.

Empty for now; kept so future fixtures/hooks land in one place. The slow-test
marker is configured via pyproject.toml; pass ``-m slow`` to opt in.
"""
