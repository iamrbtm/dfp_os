{
  "$schema": "https://opencode.ai/config.schema.json",
  "instructions": [
    "CRITICAL: Always pipe long bash outputs to '| head -n 50' or redirect to '/tmp/agent.log' to prevent RAM inflation."
  ],
  "indexing": {
    "provider": "ollama",
    "ollama": {
      "baseUrl": "http://localhost:11434"
    },
    "vectorStore": "lancedb"
  }
}